server:
  port: 8080
  
---
spring:
  profiles: local-mssql
  datasource:
    url: jdbc:mysql://localhost:3306/?useSSL=false&user=root&password=root
    username: root
    password: root
 
---
server:
  context-path: /
---
spring:
  jpa:
    properties:
      hibernate.dialect:
        org.hibernate.dialect.MySQL5Dialect
---
spring:
  datasource:
    url:
      jdbc:mysql://localhost:3306/ad_f33ba6ada231ff9?useSSL=false&user=root&password=root
